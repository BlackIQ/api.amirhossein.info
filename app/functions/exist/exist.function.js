// ----------------------------------------------
// exist.function.js
// ----------------------------------------------
// Here we check that is there any item in the array of not.

const exist = (data) => {
  // ----------------------------------------------
  // exist()
  // ----------------------------------------------
  // Just check that length is more than 0 or not.

  return data.length !== 0 ? true : false;
};

export { exist };
